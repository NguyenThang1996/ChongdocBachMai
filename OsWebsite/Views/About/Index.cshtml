@model List<OsWebsite.Models.News>
@{
    <title>@ViewBag.MetaTitle</title>
    if (ViewBag.MetaDescription != null)
    {
        <meta name="description" content="@ViewBag.MetaDescription" />
    }
    if (ViewBag.MetaKeywords != null)
    {
        <meta name="keywords" content="@ViewBag.MetaKeywords" />
    }
    var GalleryImages = ViewBag.GalleryImages as List<OsWebsite.Models.NewsImages>;
}
<div class="banner-page">

</div>
<div class="box-product">
    <div class="box-content-product-page">
        <div class="container">
            <div class="row">
                <div style="background-color: #f5f5f5; padding-top: 10px" class="col-md-12 col-lg-12 col-sm-12 col-xs-12">
                    <h4>
                        <a style="text-decoration: none; color: #000; font-weight: bold;" href="#">
                            @ViewBag.NameAbout
                        </a>
                    </h4>
                    @*<div class="box-slider-about">
                                <div class="col-md-8 col-lg-8 box-about-slider">
                                    <div id="owl-about" class="carousel slide carousel-fade carousel owl-theme">
                                        @if (GalleryImages.Count > 0)
                                        {
                                            foreach (var item in GalleryImages)
                                            {
                                                <div class="item">
                                                    <img src="@item.Images" alt="@item.Name" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="item">
                                                <img src="@ViewBag.Images" alt="@ViewBag.NameAbout" />
                                            </div>
                                        }
                                    </div>
                                </div>
                            </div>*@
                    @Html.Raw(@ViewBag.ContentNew)
                </div>                
            </div>

        </div>
    </div>
</div>